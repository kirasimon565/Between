name: Build and Deploy to GitHub Pages

on:
  push:
    branches:
      - feat/create-project-skeleton

permissions:
  contents: write # Needed to push to the gh-pages branch

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Download and Setup Tweego
        run: |
          wget https://github.com/tmedwards/tweego/releases/download/v2.1.1/tweego-2.1.1-linux-x64.zip
          unzip tweego-2.1.1-linux-x64.zip
          chmod +x tweego
          # Add tweego to the path so npm scripts can find it
          echo "$(pwd)" >> $GITHUB_PATH

      - name: Install Dependencies
        run: npm install

      - name: Build Game
        run: npm run build

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          # The user will need to enable GitHub Pages in their repo settings for the gh-pages branch.
